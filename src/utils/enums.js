export const Roles = Object.freeze({
    USER: "user",
    MODERATOR: "moderator",
    ADMIN: "admin"
});

export const Sort = Object.freeze({
    NEWEST: "newest",
    OLDEST: "oldest",
    TOP: "top"
});